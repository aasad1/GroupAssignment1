<%= form_with(model: course_assignment, local: true) do |form| %>
  <% if course_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_assignment.errors.count, "error") %> prohibited this course_assignment from being saved:</h2>

      <ul>
        <% course_assignment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section %>
    <%= form.text_field :section %>
  </div>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.collection_select :professor_id, Professor.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.collection_select :course_id, Course.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
